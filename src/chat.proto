syntax = "proto3";
package chat;

message ChatMessage {
    string from = 1;
    string message = 2;
}
message Empty {}

service ChatService {
    rpc CreateStream(ChatMessage) returns (stream ChatMessage);
    rpc BroadcastMessage(ChatMessage) returns (Empty);
}
